<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Bug fix: RECORD_AUDIO was missing entirely, even though getUserMedia({audio:true})
      is used throughout the app (ChatTab, ScenarioTab, TranslatorTab, PodcastTab,
      MatrixTab - voice input, pronunciation practice, and shadowing all need it).
      Without this declared, Android denies microphone access at the OS level before
      the WebView's runtime permission prompt can even appear, so every one of those
      features would silently fail on a real device. MODIFY_AUDIO_SETTINGS is the
      standard companion permission Android expects alongside audio capture.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!--
      Bug fix (Android device testing, issue #4 continued): on Android 11+
      (API 30+), package visibility restrictions mean PackageManager can't
      resolve the on-device speech recognition service unless the app
      declares it here - without this, @capacitor-community/speech-
      recognition's available()/start() calls can fail to find the
      recognizer even with RECORD_AUDIO granted. See lib/nativeSpeech.ts.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
